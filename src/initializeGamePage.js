export const initializeGamePageDom = (playerName) => {
  console.log(playerName);
  // make buttons fot carrier/ abtteship/curiser/subarine/ destroyer &

  makeGameBoardDOM(true); // player's grid
  // a submit button that makes another grid that shows enemy grid
  // and let's you hit computer's grid
};

function makeGameBoardDOM(isItPlayer) {
  // make a player that has it's own gameboard if isItPLayer = true
  // setup event listerner for if it's human
  // if isItPLayer == false, make another computer player and set up the ships
  //
}
